compilation_database_dir: ..
output_directory: puml
diagrams:
  {{ name }}_{{ type }}:
    type: {{ type }}
    glob:
      - ../../tests/{{ name }}/{{ name }}.cc
    using_namespace:
      - clanguml::{{ name }}
    include:
      namespaces:
        - clanguml::{{ name }}